/**
 * Naming Convention 1
 *
 * 1) 일반적으로 영어(라틴문자)를 사용하며 문자와 숫자를 모두 사용 할 수 있다.
 * 2) 특수기호는 언더스코어와 달러를 사용할 수 있다.
 * 3) 숫자로 이름의 첫 글자로 시작할 수 없다.
 * 4) 키워드는 변수명으로 사용할 수 없다.
 */

let codefactory = "코드팩토리";
var $ive = "아이브";
const _yujin = "안유진";

console.log(codefactory);
console.log($ive);
console.log(_yujin);

// let 1name = 'no' -> 에러 발생
// let const = '123' -> 에러 발생

/**
 * Naming Convention 2
 *
 * 1) camelCase -> 대부분의 언어에서 많이 사용
 * 2) snake_case -> 파이썬 같은 언어에서 사용
 * 3) PascalCase -> c# 마이크로소프트 계열의 언어에서 사용, 별로 안 씀
 */
